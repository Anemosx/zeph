version: '3.8'

services:
  zeph:
    build: .
    ports:
      - "8088:8088"
      - "0.0.0.0:8088:8088"
    env_file:
      - .env
    environment:
      - SPOTIFY_CLIENT_ID=${SPOTIFY_CLIENT_ID}
      - SPOTIFY_CLIENT_SECRET=${SPOTIFY_CLIENT_SECRET}
      - VERBOSE=${VERBOSE:-False}
